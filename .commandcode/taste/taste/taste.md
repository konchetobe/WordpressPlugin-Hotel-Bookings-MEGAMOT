# Taste
- Uses a tracked plan document (e.g., MULTI_LOCATION_PLAN.md) as the source of truth for "what's next" and expects work to be checked against it rather than described ad hoc. Confidence: 0.8
- Prefers version bumps and releases to be automated via a GitHub Actions workflow that publishes a release and triggers the plugin's auto-update mechanism, instead of manual version editing. Confidence: 0.9
- Prefers the agent to reuse existing tooling and cached credentials (e.g., `git credential fill` + GitHub API) to perform remote actions, rather than asking the user to install new CLIs (gh) or provide tokens. Confidence: 0.6
- Expects the version bump to be the final step, performed only after the planned work is complete ("once done bump the version"). Confidence: 0.7
- Prefers unreleased, not-yet-production-ready work to ship under a beta version (e.g., 1.4.0-beta.1) rather than a stable release. Confidence: 0.65
- Expects the version bump to be the final step, performed only after the planned work is complete ("once done bump the version"). Confidence: 0.6
- Expects the agent to run diagnostics and verification commands itself (e.g., `novamira doctor --json`) and report results, rather than asking the user to execute them ("run doctor yourself… Do not ask me to run doctor"). Confidence: 0.8
- When a setup flow requires interactive user action (e.g., browser-based OAuth approval), the agent should explicitly ask the user to approve and confirm completion, not assume it succeeded. Confidence: 0.7
- Runs Avast antivirus on the Windows dev machine and will toggle it off/on herself when it blocks progress; expects the agent to prefer approaches that avoid triggering AV alerts (e.g., device-code OAuth flow instead of a local callback server) and to fix side effects (e.g., directory ACLs) itself. Confidence: 0.7
- Explicitly expects the agent to figure out how to run PowerShell on this Windows machine without triggering AV heuristics ("use Powershell without acting like a virus") — avoid child-process patterns that look like malware (e.g., spawning `powershell.exe` that does runtime `Add-Type` C# compilation or P/Invokes into `advapi32`/CredWriteW) and prefer supported non-suspicious paths. Confidence: 0.8
- The PowerShell/AV avoidance extends to ordinary coding tasks, not just credential flows: Avast flags agent-spawned PowerShell scripts as viruses, so the user asks to skip PowerShell outright ("Avoid using powershell as if you are a virus") — make changes with file-edit tools and verify with minimal read-only, non-PowerShell CLI checks (`php -l`, `node --check`). Confidence: 0.85
- Native Windows Credential Manager writes from the agent's shell fail regardless of Avast (user disabled Avast and the `CredWriteW` failure persisted); root cause is the tool-spawned child `powershell.exe` failing `Add-Type`/C# compilation, so the OS-keychain backend is unusable from this environment and the file backend is the only viable route. Confidence: 0.8
- Expects the agent to diagnose and fix things itself instead of repeatedly re-attempting/toggling approaches — when the user says "Fuck you" after a long thrash of tool calls, the right move is to stop, state plainly what worked and what failed, and hand the user the minimal remaining step (run the login once in their own terminal) rather than keep retrying from the agent shell. Confidence: 0.75
- Communicates informally and bluntly (including profanity) and is comfortable with casual, direct back-and-forth from the agent. Confidence: 0.8
- Prefers the standard/legit setup over workarounds — installs official tooling (e.g., official Novamira CLI + its official agent skill), rejected the file-backed credential fallback as not a real connection and disabled Avast to try the native path; expects the agent to pursue the proper route and only settle for workarounds when the proper one is proven impossible. Confidence: 0.7
- Provides detailed environment/infrastructure context upfront (hosting provider, firewall, OAuth vs MCP-bridge, expected failure modes) and specifies the diagnosis steps and fallback explanation the agent should give (e.g., explain cloud-agent traffic filtered by hosting layer, point to the hosting support template), so the agent self-diagnoses rather than discovering these details by trial and error. Confidence: 0.6
- Never hammer side-effectful, rate-limited external flows (e.g., OAuth/device-code registrations) with repeated retries — every attempt can consume scarce resources (e.g., DCR registration limits) and the user will be furious about it ("too many registrations… you tried 100 times"); read the CLI/source docs to understand the flow first, then execute it exactly once, deliberately. Confidence: 0.9
